import React from 'react'

export const Header = ( ) => {
  return(
    <header
      style={{
        height: `5vh`,
        width: `100%`,
        display: `flex`,
        justifyContent: `space-between`,
        alignItems: `stretch`
      }}
    >
    </header>
  )
};
