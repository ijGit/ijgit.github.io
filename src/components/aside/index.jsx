import React from 'react';
import styled from "styled-components"
import {Bio} from './../bio'

const Container = styled.aside`
  display: flex;

`
  
  
  export const Aside = () => {

  return (
    <Container>
    </Container>
  )
}
